services:
  server:
    build: .
    ports:
      - "${SERVICE_PORT}:8080"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./calls:/app/calls
    restart: unless-stopped
